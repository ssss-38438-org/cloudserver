const assert = require('assert');


describe('Complete MPU - handling fuzzy metadata responses i.e 500', () => {

    it('should compose the object when the op is retried - obj created in MD', () => {

    });

    it('should compose the object when the op is retried - obj not created in MD', () => {

    });

});
